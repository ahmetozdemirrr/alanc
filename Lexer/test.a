include stdio

int main()
{
    array[] = {2, 3, 4}

    function foo(int a, float b)
    {
        str string = ahmet
        a + b = b or a
    }

    procedure poo()
    {
        bool b1, b2 ;
        b1 -> b2
    }
}

# This is a comment but \n now this is a expression ; x = y + 5
